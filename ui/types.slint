export struct PersonData {
    id: int,
    name: string,
    surname: string,
    rank: string,
    methodology: color    
}

export struct GroupData {
    id: int,
    name: string,
    members: [PersonData]
}
